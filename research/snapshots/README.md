# Snapshots

Optional point-in-time captures (large HTML/PDF exports, screenshot sets). Stored under `YYYY-MM/` subfolders.

**Gitignored** by default — see root `.gitignore`. Keep curated summaries in `knowledge-base/` instead.
